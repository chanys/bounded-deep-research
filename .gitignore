# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
*.egg-info/

# Node
node_modules/
.next/
.turbo/

# IDE
.idea/
.vscode/

# Claude Code (commit .claude/settings.json; keep personal overrides local)
.claude/settings.local.json

# Env
.env
.env.local

# Data
/data/*
!/data/.gitkeep

# OS
.DS_Store
traces

# Private planning notes (not shared)
planning_docs

# corpus snapshot for loading into RDS (large binary; never commit)
corpus.dump

# Bulk generated eval data (per-video summaries, question pool, machine inventory,
# query candidates) belongs in S3/backup, not core git; only the small human-readable
# inventory .md, the reviewed query set, and review CSVs are tracked.
/eval/artifacts/*.jsonl
/eval/artifacts/*.json
!/eval/artifacts/queryset_*.jsonl
